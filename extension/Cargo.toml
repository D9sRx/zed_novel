[package]
name = "novel_reader_extension"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
zed_extension_api = "0.7.0"

[profile.release]
lto = true
strip = true
codegen-units = 1
